# 作業ログ

## 2026-05-15 17:30
### 採番モードの解除方法の追加
- ページ内をクリックしても採番モードを解除できるように改善。
- **実装内容**:
    - `content-hint.js`: ページ全体（`window`）に `mousedown` イベントリスナーを追加。モードが有効な時にクリックが発生した場合、`isNumberingMode` を `false` に設定してモードを終了するようにした。

## 2026-05-15 17:40
### ページ内採番入力ボックスの実装
- 採番モード有効時に、ページ中央上部にフローティングの入力ボックスを表示するようにした。
- **実装内容**:
    - `content-hint.js`: 
        - プレミアムなデザイン（グラスモルフィズム、アニメーション）の入力ボックスを動的に生成。
        - モード開始時に自動でフォーカス。
        - `Enter` で確定実行、`Escape` でキャンセル。
        - サイドパネル側の入力欄とも連動（同期）。

## 2026-05-15 17:55
### 採番UIの改善とサイドパネルからの削除
- ページ内の採番入力ボックスの視認性を向上させ、サイドパネルから入力欄を削除した。
- **実装内容**:
    - `content-hint.js`: 
        - 背景色を白（不透明度高め）に固定し、文字色を黒に固定。
        - カーソル（caret-color）を黒に設定して視認性を確保。
        - ダークモード対応を解除し、常に一定のデザインで見えるように修正。
    - `sidepanel.html`: サイドパネル内の採番入力欄（`#numberingInput`）とデータリスト（`#numberingDataList`）を削除。
    - `sidepanel.js`: サイドパネル内の入力欄に関連する処理（フォーカス制御やリスト更新）を削除。

## 2026-05-15 18:00
### 採番ボックスのドラッグ＆ドロップ対応
- ページ内の採番入力ボックスをドラッグで自由に移動できるように改善。
- **実装内容**:
    - `content-hint.js`: 
        - ボックスのドラッグ移動ロジックを実装。
        - ボックス自体をクリック・ドラッグした際には採番モードが解除されないようにイベント処理を修正。
        - ドラッグ中はカーソルを `grabbing` に変更し、直感的な操作感を実現。

## 2026-05-15 18:15
### プルダウン選択機能の追加
- ページ内の採番入力ボックスにおいて、番号入力だけでなくリストからの選択も可能にした。
- **実装内容**:
    - `content-hint.js`: 
        - `datalist` 要素を導入し、ページ内のクリック可能要素をプルダウン形式で表示。
        - `renderHints` 実行時に `datalist` の内容を動的に更新。
        - リストから項目が選択された際に即座にクリックが実行されるようにイベント処理を改善。

## 2026-05-15 18:25
### 採番UIの視認性向上（外枠の追加）
- ページ内の採番入力ボックスをより際立たせるため、はっきりとした外枠を追加。
- **実装内容**:
    - `content-hint.js`: 入力ボックスの外枠を `2px solid #3b82f6`（アクセントカラーの青）に変更。
    - `content-hint.js`: プルダウンの不必要な更新を抑制し、選択中にリストが閉じてしまう問題を修正。

## 2026-05-17 14:00
### YouTube 右サイドバー配置機能の改良 (ネイティブ属性の制御)
- カスタムCSSでのレイアウト強制による崩れを防ぐため、YouTube標準の1カラム・2カラム判定属性（`is-two-columns_`）を直接フックして制御するように改良。
- **実装内容**:
    - `content-youtube.js`:
        - `applyYtSidebarBreakpoint` 関数を全面リファクタリングし、CSSのインジェクションを廃止。
        - `ytd-watch-flexy` (および新UIの `ytd-watch-grid`) 要素の `is-two-columns_` 属性を動的にセット/削除するように変更。
        - YouTubeの純正JSによる書き戻しを防ぐため、`MutationObserver` でこの属性の変化を監視し、設定したブレークポイントに基づき常に強制上書きするよう実装。
        - 属性変更時およびデフォルト復元時に、`window.dispatchEvent(new Event('resize'))` を発火させることで、YouTube純正のレスポンシブサイズ計算ロジックを強制的にトリガー。これにより、プレイヤーのアスペクト比や概要欄の幅が完全にYouTubeネイティブのままで綺麗に追従するようにした。

## 2026-05-17 14:15
### YouTube 右サイドバー配置の「1400px以下で下」固定ON/OFFトグル化
- 複数の画面幅の選択肢を廃止し、最も実用的な「1400px以下で下に回り込む」挙動に固定した上で、ON/OFFをトグルで簡単に切り替えられるようにシンプルなUI・ロジックへ変更。
- **実装内容**:
    - `sidepanel.html`:
        - `select` ドロップダウンによる画面幅選択を廃止。
        - 代わりにチェックボックス型のトグルスイッチ（`#toggleYtSidebarBreakpoint`）に変更。
        - 説明文を「1400px以下のときに右サイドバーを下に回り込ませます」に更新。
    - `sidepanel.js`:
        - `configs` 内の登録キーを `ytSidebarBreakpointEnabled`（初期値 `false` のboolean）に変更し、トグルのON/OFF状態をストレージに同期するように修正。
    - `content-youtube.js`:
        - 変数 `ytSidebarBreakpointEnabled` でトグルのON/OFF状態を監視。
        - トグルONのときは閾値を「1400px」に固定してネイティブ属性（`is-two-columns_`）の制御およびリサイズ発火を行い、トグルOFFのときはYouTube純正のレイアウト（デフォルトの1000px）へ即座に戻すシンプルな制御にリファクタリング。

## 2026-05-17 14:20
### YouTube サイドバー配置トグルOFF時の即座反映対応
- トグルをOFFにした際、即座にYouTube本来の画面幅判定（デフォルト約1000px）に基づいた正しいレイアウトにリサイズされるように修正。
- **実装内容**:
    - `sidepanel.html`: ユーザー操作による説明文の「基準値」への文言変更を確認。
    - `content-youtube.js`:
        - トグルOFFになった際、単に `resize` イベントを発火させるだけでなく、現在のウィンドウ幅から算出されるYouTube本来の属性（ウインドウ幅1000px以上なら `is-two-columns_` あり、未満ならなし）を手動で先に反映させてから `resize` イベントを発火させるように修正。
        - これにより、トグルをOFFにした瞬間にリフレッシュすることなく、即座に本来あるべきレイアウト（2カラム等）へと元通り再計算されるようになった。

## 2026-05-17 14:30
### YouTube 右サイドバー配置の「常に下」固定化
- トグルONの際は、画面幅に関わらず「常に動画の下（1カラム）」にサイドバーが回り込むように仕様変更。
- **実装内容**:
    - `sidepanel.html`: 説明文を「右サイドバーを常に動画の下に配置します」に更新。
    - `content-youtube.js`:
        - トグルONの時の `shouldBeTwoColumns` 判定を、画面幅のチェック（1400pxなど）を行わず、常に `false`（1カラム＝サイドバー下）で固定するようにリファクタリング。
        - これにより、トグルONの間はどのようなウインドウ幅でも完全に右サイドバーが動画下に回り込むようになった。

## 2026-05-17 14:55
### YouTube 右サイドバー配置トグルON時の動画真っ暗バグ（無限ループ）の修正
- トグルON状態でウィンドウサイズを変更し、1000pxの境界を跨いだ際に動画が真っ暗になる（クラッシュする）重大な不具合を修正。
- **原因と対応**:
    - YouTube純正スクリプトと本拡張機能の間で `is-two-columns_` 属性の付与・削除と `resize` イベント発火が無限ループ（Call Stack Exceeded等）を起こし、動画プレイヤーのレンダリングが破壊されていたのが原因。
    - `content-youtube.js`: `MutationObserver` 経由での属性上書き時には `window.dispatchEvent(new Event('resize'))` を絶対に発火させないよう `isFromObserver` フラグを導入し、ループを完全に遮断。
    - また、ループの温床となっていた不要な `window.addEventListener('resize')` 自体の登録を削除。YouTube純正のリサイズ検知に任せ、そこから派生した属性変化のみをObserverで安全に刈り取る堅牢な設計に変更。

## 2026-05-17 15:10
### YouTube 右サイドバー配置の動画真っ暗バグ（Polymerライフサイクル衝突）の完全修正
- 先の対応でも「境界線を越えた際に動画が真っ暗になる（再度リサイズすると直る）」現象が解消しなかったため、根本原因（非同期タイミングバグ）を特定して完全に修正。
- **原因と対応**:
    - 真っ暗になる真の原因は、YouTube内部のUIコンポーネント（Polymer）が画面幅の変化を検知して内部状態（2カラム）の再レンダリング処理を行っている「まさにその瞬間（同期的）」に、拡張機能が `is-two-columns_` 属性を強制的に引剥がしてしまうことで、PolymerのデータバインディングとDOM状態が不整合を起こし、動画プレイヤー（`<video>`）の初期化プロセスがクラッシュすることにあった。
    - `content-youtube.js`: 属性の上書き処理に **非同期の遅延（setTimeout 100ms）** と **排他ロック（isApplying）** を導入。
    - YouTubeの内部処理が完全に落ち着くのを100ms待ってから属性を上書きし、その上で安全に `resize` イベントを発火させることで、真っ暗になる（クラッシュする）ことなく、どんな画面幅からリサイズしても常に美しい1カラム表示を強制できるようになった。

## 2026-05-17 18:25
### マウスホイールによるテキスト選択機能の実装
- Shift+マウスホイールで入力欄（input, textarea）の文字選択を伸縮する機能を新規実装。
- **実装内容**:
    - `content-wheel-select.js`（新規作成）：Shift+ホイール操作を検知し、`setSelectionRange()` で選択範囲を伸縮するロジックを実装。ストレージの `wheelSelectEnabled` フラグでON/OFF制御。
    - `manifest.json`：`<all_urls>` のコンテンツスクリプトに `content-wheel-select.js` を追加。
    - `sidepanel.html`：ツール設定パネルにトグルスイッチ（`#toggleWheelSelect`）を追加。
    - `sidepanel.js`：`loadToolsSettings` の `configs` 配列に `wheelSelectEnabled` エントリを追加。機能一覧にも追記。

## 2026-05-17 18:32
### マウスホイールテキスト選択機能のデバッグ対応
- 動かない不具合に対して、詳細なデバッグログ（Console.log）を `content-wheel-select.js` に追加し、さらにイベントリスナーの登録・解除オプション（capture, passive）を完全に整合させた。

## 2026-05-17 18:38
### アンカーベースのスマート選択伸縮ロジックへの刷新
- カーソルがテキスト末尾にある等の境界値で選択範囲が伸縮しない不具合を解消するため、`selectionchange` イベントによる「選択開始アンカー」の自動追跡ロジックを導入。
- これにより、キーボードの `Shift + ArrowKey` と同等に、ホイールの上下で左方向・右方向のどちらにも直感的に選択を拡大・縮小できるスマートな文字選択動作を実現。

## 2026-05-17 18:41
### ダイヤル式文字入力＆クリック確定機能への全面刷新
- ユーザーの本来の意図である「マウスホイールをダイヤルのように回して文字を切り替え、左クリックで確定する入力機能」へと仕様を全面リニューアル。
- **実装内容**:
    - `DRUMS`（英小文字、英大文字、数字、ひらがな）を定義し、現在選択中の1文字をホイールのスクロールで前後の文字へ循環して切り替えるロジックを実装。
    - カーソルのみの状態でホイールを回すと、新規に初期値 `'a'` が挿入され、それを選択状態（ダイヤル入力中）にする挙動を実装。
    - ダイヤル選択状態の文字がある中で「左クリック」すると、選択が解除されてカーソルがその文字の直後に移動（確定）するクリックハンドラを実装。
    - ReactやVue等のWebアプリケーションでの入力値バインディングに対応するため、文字変更時に `input`/`change` イベントを動的に発火させる設計を導入。

## 2026-05-17 18:49
### Shift+ホイールのみによる「英小文字→英大文字→数字→ひらがな」統合ドラムへの簡素化
- ユーザー指示に基づき、複雑なCtrl/Alt等のショートカットキーを廃止し、操作キーを「Shift + ホイール」の単一キーに完全に統合。
- **実装内容**:
    - 開始文字は常に `'a'` からスタート。
    - そこから上にホイールを回し続けるだけで、`英小文字(a-z) → 英大文字(A-Z) → 数字(0-9) → ひらがな(あ-ん)` の順番でシームレスに行き来できるようにロジックを簡素化。
    - 逆に下にホイールを回すと逆順（`a → ひらがな(ん-あ) → 数字 → 英大文字`）に瞬時にアクセス可能。最もシンプルで迷わない操作性を実現。

## 2026-05-17 18:52
### ひらがなドラムへの小書き文字（ゃゅょっ）と長音記号（ー）の追加
- 日本語の拗音・促音入力（「ちゃ」「しゅ」「ちょっと」等）や、カタカナ・外来語的表現の表記をサポートするため、ひらがなドラムの文字セットを拡充。
- **実装内容**:
    - `やゆよ` の直後に `ゃゅょ` を配置し、スムーズに拗音を選択できるよう改善。
    - ドラム末尾（`ん` の後ろ）に促音 `っ` と長音 `ー` を追加し、逆スクロールからでも瞬時に呼び出せる超実用的な配置に最適化。

## 2026-05-17 18:55
### 修飾キー判定の厳格化によるブラウザ標準ショートカットの競合防止
- `Ctrl + Shift + ホイール` や `Alt + Shift + ホイール` など、他の修飾キーが同時に押されている場合は本拡張機能を動作させないようにガード文を強化。
- **実装内容**:
    - `e.ctrlKey`、`e.altKey`、`e.metaKey` のいずれかが `true` の場合は即時に `return` する処理を追加。
    - これにより、他のキーボードショートカットやブラウザ標準のズーム等の機能を一切損なわず、「Shiftキー単体での押し下げ」の時だけ正確に文字ダイヤル入力が機能する極めて安全な設計に改善。

## 2026-05-17 18:58
### ホバー検出によるShiftキー完全不要化（極限のプレミアム入力体験）
- キーボード操作すら不要にし、片手（マウスのみ）で完全に文字入力を完結させたいというユーザー指示に基づき、ホバーベースのホイール文字ダイヤル入力へ進化。
- **実装内容**:
    - イベントターゲット（`e.target`）のホバー検出を導入し、マウスカーソルが `input` や `textarea` 要素に乗っている時のみ、キーの押し下げなし（Shift不要）で直接ホイール入力が起動するロジックを実装。
    - 入力要素以外でのスクロールは100%通常のページスクロールとしてパススルーされ、完全に競合を回避。
    - ホバー時に自動的に要素にフォーカスを当てる（`.focus()`）ことで、入力可能な状態へのシームレスな移行を実現。

## 2026-05-17 19:01
### 本機能の初期設定（デフォルト）がOFFであることを保証
- 新規導入時や初期状態において本機能が勝手に動作して通常のユーザー体験を妨げないよう、デフォルト値を完全にOFF（無効）に設定していることを確認・保証。
- **設定値**:
    - `sidepanel.js` 内の configs 配列: `{ id: 'toggleWheelSelect', key: 'wheelSelectEnabled', default: false }`
    - `content-wheel-select.js` 内のストレージ取得初期値: `chrome.storage.local.get({ wheelSelectEnabled: false }, ...)`
    - これにより、ユーザーがサイドパネルのトグルスイッチで明示的にONにした場合のみ機能する安全な挙動を保証。

## 2026-05-17 19:08
### マウスホイール押し込みによる漢字変換候補表示（中クリックIME）の実装
- ホバー入力したひらがなをスムーズに漢字やカタカナに変換できるよう、ホイール押し込み（中クリック）による日本語変換支援機能を新規実装。
- **実装内容**:
    - **オートスクロールの安全な抑止**: `mousedown` 時に中クリック（`button === 1`）を検出し、ブラウザ標準のオートスクロール動作を完璧にブロック。
    - **直前のひらがな連続抽出**: カーソル位置から左に向けてひらがな（小書き・促音・長音含む）を自動スキャンし、変換対象のひらがな文字列を正確に特定。
    - **Google Transliterate API連携**: 特定したひらがなをAPI経由で非同期に投げて、高精度な漢字変換候補を取得。
    - **ガラスモルフィズム風ポップアップ描画**: 入力欄の直下に、ダークモード対応かつ滑らかな影を持つプレミアムな変換候補ポップアップを動的に生成。
    - **候補のクリック置換**: 候補をクリックすると、直前のひらがな部分を瞬時にその漢字へ置換し、カーソルを最適位置へセットして React 等の State 同期イベントを発火。
