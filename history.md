# 作業ログ

## 2026-04-29 10:13
### 不要なログ出力の削除と整理
- アプリ内の動作確認用 `console.log` や不要な `console.error` を整理しました。
- `content-ai.js`: GeminiCanvasFullscreen 関連の開発用ログ (`console.log`) を削除し、コメントに変更。
- `background.js`: `catch` 句の `console.error` を空関数に変更してコンソール出力を抑制。
## 2026-04-26 17:40
- 拡張機能の説明文（ストア掲載用）を現在の実装状況（AI音声入力削除、レスポンシブUI、Gemini全画面等）に合わせて修正


## 2026-04-26 20:20
- タブパネル背景へのブックマークドロップ機能を大幅強化
  - `dragover` に加え `dragenter` イベントでもドロップ許可（`preventDefault`）を行うように修正し、ブラウザの判定を確実化。
  - `setupGlobalDragAndDrop` に処理を統合し、`document` レベルで全域を監視。
  - タブアイテム以外（背景部分）でのドロップを優先的に処理するようにロジックを整理。

## 2026-04-26 20:06
- ブックマークとフォルダに削除ボタンを追加
  - `sidepanel.js`:
    - フォルダとブックマークの各アイテムに削除ボタン（`bookmark-delete-btn`）を追加
    - 削除ボタンクリック時のイベントハンドラ（`handleBookmarkDelete`）を実装
    - 右クリックメニューの削除処理も共通化
  - `sidepanel.css`: 削除ボタンのスタイリングを追加。ホバー時に表示されるように設定。

## 2026-04-26 16:16
- 空のブックマークフォルダを表示するように変更
  - `sidepanel.js`: `renderBookmarkNode` 内の空フォルダをスキップする判定を削除

## 2026-04-26 16:15
- ブックマークフォルダの作成・削除機能を実装
  - `sidepanel.html`: ブックマークパネル上部に「新規フォルダ作成」ボタンを追加
  - `sidepanel.js`:
    - 新規フォルダ作成ボタン（`newFolderBtn`）のイベントリスナー実装
    - フォルダのコンテキストメニューに「この中にフォルダを作成」と「フォルダを削除」を追加
    - `chrome.bookmarks.create` および `chrome.bookmarks.removeTree` を使用したロジックの実装
    - 削除時の確認ダイアログ（`confirm`）を追加

## 2026-04-26 14:47
### タブを背景にドロップして新規ウィンドウに切り離す機能
- タブパネルの空白エリア（背景部分）にタブをドラッグ＆ドロップすると、そのタブを新しいウィンドウに切り離す機能を追加。
- `sidepanel.js`:
  - `setupTabDetach` 関数を新設。`document` レベルで `dragover` / `drop` をリスンし、初期化時に1回だけ登録。
  - タブドラッグ中（`draggedTabId` がセットされている状態）に `.tab-item` 以外へのドロップで `chrome.windows.create({ tabId })` を呼び出す。
  - `.tab-item` の `drop` ハンドラに `e.stopPropagation()` を追加し、並び替えドロップ時の二重処理を防止。
  - 過去の試行: `tabsList` → `tabsPanel` → `document` と対象を変更。パネルの高さがコンテンツにフィットするため背景エリアが存在しなかった問題を解消。


## 2026-04-26 12:25
### タブタイトルの表示領域の拡大
- タブタイトルの表示スペースを最大化するため、レイアウトの隙間と余白を最適化しました。
- `sidepanel.css`:
  - `.tab-item`: `gap` を 8px から 6px に縮小し、`padding` を 8px に調整。
  - `.mute-control`: 非表示時にスペースを占有しないよう `display: none` に変更。また、`margin-left: auto` を削除し、`.tab-item-info` の `flex: 1` によってボタン類を右端に寄せる構成に変更。
- これにより、分割表示などの幅が狭い状態でも、より長いタイトルが表示可能になりました。

## 2026-04-26 12:18
### ウィンドウセパレーターのデザイン簡略化と中央揃え
- ウィンドウセパレーターの左右の横線を削除しました。
- 「WINDOW X」ラベルと「＋」ボタンを中央に配置するように変更し、よりクリーンなデザインにしました。
- `sidepanel.css`: `.window-separator` に `justify-content: center` を追加し、`::before` / `::after` のスタイルを削除。

## 2026-04-26 12:15
### グローバルな「新規タブを追加」ボタンの削除
- 各ウィンドウに個別ボタンが追加されたため、タブパネル最上部の大きな「＋新規タブを追加」ボタンを削除しました。
- `sidepanel.html`: `#newTabBtn` を含む `.panel-actions` セクションを削除。
- `sidepanel.js`:
  - `setupNewTabAction` の呼び出しと定義を削除。
  - `applySplitRatio` 内のボタンテキスト更新ロジックを削除。
- `sidepanel.css`: 使用されなくなった `.panel-actions` 関連のスタイルをクリーンアップ。

## 2026-04-26 11:55
### ウィンドウごとの「新規タブを追加」ボタンの実装
- 各ウィンドウセパレーターに「新規タブを追加」ボタン（＋アイコン）を追加しました。
- スペースを節約するため、ウィンドウボタンはアイコンのみの非常にコンパクトなデザイン（18x18px）に設定しました。
- パネル幅が極端に狭い「ミニマルモード」時にも、セパレーターの余白をさらに詰め、操作性と省スペース性を両立させました。
- `sidepanel.js`:
  - `renderTabs` 内で、1ウィンドウの場合も含め常にウィンドウセパレーターを表示するように変更し、ボタンを注入。
  - 各ボタンに `windowId` を持たせ、対象のウィンドウで新しいタブを開くようにイベントリスナーを設定。
- `sidepanel.css`:
  - `.window-new-tab-btn` のスタイルを追加。
  - `.mode-minimal` 時のセパレーター余白・ラベルサイズを調整。

2: 
3: ## 2026-04-26 10:59
4: ### ブックマークとタブのグループ視認性向上
5: - ブックマークが「favicon-only」モード（分割表示で幅が狭い時）において、フォルダの終わりに水平の区切り線を表示するように変更。これにより、同じフォルダに属するアイテムの範囲が明確になりました。
6: - タブのウィンドウセパレーターに控えめな水平線を追加し、「WINDOW 1」などのラベルがより目立つように修正。
7: 

## 2026-04-26 10:45
### タブ管理パネルのレスポンシブ連動の実装
- 「＋新規タブを追加」ボタンの状態（テキスト長）に応じて、ウィンドウ名とタブ表示内容を連動させる機能を追加。
- 「ピン止め」「削除」ボタンをホバー時だけでなく、全モード（minimalを除く）で常時表示するように変更。
- `sidepanel.js`:
  - `applySplitRatio` を更新し、4つのモード（large, normal, compact, minimal）を導入。
  - ウィンドウラベルをユーザー指定の表記（WINDOW / WIN / W）に統一・自動切替。
  - パネルの幅に応じて適切なモードクラスを付与。
- `sidepanel.css`:
  - `mode-compact`: タイトルを非表示にし、ピン留め・削除ボタンをファビコンのすぐ右側に配置（左寄せ）。
  - `mode-minimal`: ファビコンのみを表示（従来の favicon-only 相当）。
### 「新規タブ追加」ボタンの改行防止とタイミング調整
- 文章作成時の「共有」メニュー内などにボタンが誤って注入される問題を修正。
- 注入対象をツールバーやヘッダー内に限定し、メニュー属性を持つ要素を除外するようにロジックを強化。
- `sidepanel.css`: `.action-btn` に `white-space: nowrap` を追加し、ボタン内のテキストが改行されないように修正。
- `sidepanel.js`: 
  - ボタンテキストが切り替わる閾値（`tabWidth`）の箇所を特定。
  - ウィンドウセパレーターのラベル（「ウィンドウ 1」等）を幅に応じて「Win 1」「W 1」に短縮するロジックを追加。
  - ウィンドウセパレーターからタブ数と横線を削除し、ラベルを中央寄せに配置。
- `sidepanel.js` / `sidepanel.css`:
  - 各タブにピン留め切り替えボタンを追加。
  - ファビコンのみの表示モード（favicon-only）時には、閉じるボタンと合わせてピン留めボタンも非表示になるよう設定。

## 2026-04-24 22:16 (JST)
### プライバシーポリシーの更新
- `privacy_policy.md`: 
  - AI要約およびGemini APIに関する記述を削除。
  - すべてのデータ処理がローカル（ブラウザ内）で完結し、外部送信を行わないことを明記。

## 2026-04-24 22:10 (JST)
### 設定メニュー（Gemini APIキー設定）の完全削除
- `sidepanel.html`:
  - ナビゲーションバーから「設定」ボタンを削除。
  - `settingsContainer`（APIキー入力やモデル選択）を完全に削除。
- `sidepanel.js`:
  - `setupSettings` 関数およびその初期化呼び出しを削除。
  - `switchTab` 内の設定パネル制御ロジックを削除。
  - 不要になったAPIキー保存・モデル確認ロジックを削除。

## 2026-04-24 22:05 (JST)
### AI要約機能の削除とページテキスト取得ツールへの変更
- `sidepanel.html`:
  - ツール名を「AIページ要約」から「ページテキスト取得」に変更。
  - 「要約する」ボタンおよびモード選択（標準/箇条書き等）を削除。
  - 「テキストをMD形式で取得」ボタンをメインアクションに変更。
  - プレースホルダーテキストを更新。
- `sidepanel.js`:
  - `sendTextToGemini` などのAI要約ロジックを完全に削除。
  - `setupSummaryTool` を更新し、ボタンクリック時にテキストを取得してテキストエリアに表示するように変更。
  - クリップボードへのコピー機能は維持。

## 2026-04-24 21:40 (JST)
### AI音声入力機能の削除
- `manifest.json`: `audioCapture` 権限の削除、説明文の更新。
- `privacy_policy.md`: マイク入力・音声データに関する記述を削除（日本語・英語）。
- `sidepanel.html`: AI音声入力ツールのUIセクション（ボタン、ステータス、プレビュー、設定項目など）を完全に削除。
- `sidepanel.js`: 
  - 音声入力に関連する変数（`mediaRecorder`, `audioChunks`等）を削除。
  - `setupVoiceTool`, `blobToBase64`, `sendToGemini` (音声版) を削除。
  - `setupOutputTabs` を要約ツール専用に修正し、DOM参照エラーを解決。
